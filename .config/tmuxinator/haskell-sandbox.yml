# ~/.tmuxinator/mowa.yml

name: haskell-sandbox
root: ~/doc/devel/haskell-sandbox

windows:
    - dev:
        layout: even-horizontal
        panes:
            - ghcid
            - stack ghci haskell-sandbox:lib
    - scratch:
        layout: main-vertical
        panes:
          - #empty
          - #empty
